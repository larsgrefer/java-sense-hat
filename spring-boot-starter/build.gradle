plugins {
    id "java"
    id "io.freefair.maven-publish-java"
    id "io.freefair.github.package-registry-maven-publish"
}

archivesBaseName = "sense-hat-spring-boot-starter"
publishing.publications.mavenJava.artifactId = 'sense-hat-spring-boot-starter'

dependencies {
    compile "org.springframework.boot:spring-boot-starter:2.1.8.RELEASE"
    compile project(":sense-hat")
}
