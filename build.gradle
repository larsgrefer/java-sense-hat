plugins {
    id "io.freefair.git-version" version "4.1.4"
    id "io.freefair.maven-publish-java" version "4.1.4" apply false
    id "io.freefair.github.base" version "4.1.3"
}

allprojects {
    repositories {
        mavenCentral()
    }

    group = "de.larsgrefer.sense-hat"
}

github {
    slug = "larsgrefer/java-sense-hat"
    username = findProperty('githubUsername')
    token = findProperty('githubToken')
}
