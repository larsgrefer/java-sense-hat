plugins {
    id "io.freefair.git-version" version "4.1.0"
    id "io.freefair.maven-publish-java" version "4.1.1" apply false
    id "io.freefair.github.base" version "4.1.0"
}

allprojects {
    repositories {
        mavenCentral()
    }

    group = "de.larsgrefer.sense-hat"
}

github {
    slug = "larsgrefer/java-sense-hat"
    username = findProperty('githubUsername')
    token = findProperty('githubToken')
}
